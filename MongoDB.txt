use school

db.students.insertOne({ name: "Anshika", age: 22, course: "AI" })

db.students.find({ name: "Anshika" })           // Find specific
db.students.find().pretty()                     // Find all, formatted

db.students.updateOne(
  { name: "Anshika" },
  { $set: { age: 23 } }
)

db.students.deleteOne({ name: "Anshika" })
